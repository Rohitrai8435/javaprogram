import java.util.*;
import java.util.List;


class GFG
{
    public static void main (String[] args)
    { ArrayList<Integer> list= new ArrayList<>();
        for(int i=0;i<a.length;i++)
        list.add(a[i]);
       for(int i=0;i<a.length;i++)
       {
           int frqc=Collections.frequency(list,a[i]);
           int s=(int)Math.floor(size/2);
          System.out.println(s);
           if((frqc)>(s)&&frqc>2);
       return a[i];
       }
       return -1;
        
    }
}
